# Fully-pinned runtime dependencies (direct + transitive), resolved for the
# minimum supported interpreter (Python 3.10, required by Anki 26.08).
# Every entry MUST be available as a pure-Python (none-any) wheel; the build
# fails loudly otherwise. Regenerate deliberately - this is the lockfile.

# Web framework (pydantic stays on v1: pydantic 2 needs compiled pydantic-core)
fastapi==0.109.2
starlette==0.36.3
pydantic==1.10.22
typing_extensions==4.16.0

# ASGI server (pure-Python: no uvloop/httptools)
uvicorn==0.30.6
h11==0.16.0
click==8.5.0
colorama==0.4.6

# anyio stack (starlette dependency)
anyio==4.15.1
idna==3.20
exceptiongroup==1.2.2

# Query DSL
lark==1.2.2
glom==24.11.0
boltons==24.0.0
face==24.0.0
attrs==24.3.0
